# Turn Aiurinto a slave of the local swarm in whichCharis leader (the command totake control ofAiuris not requested).

docker-machine ssh Aiur "docker swarm join --token `docker swarm join-token worker -q` `docker-machine ip Char`:2377"
